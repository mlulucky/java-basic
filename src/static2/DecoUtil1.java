package static2;

public class DecoUtil1 {
  public String deco(String str) {
    return "*" + str + "*";
  }
  // 인스턴스 메서드는 인스턴스 변수를 활용하는 목적이 큰데,
  // 메서드내 멤버변수가 없다. 기능만 있다.
  // 인스턴스 생성 후 사용하기에는 객체생성 의미가 없다.

}
